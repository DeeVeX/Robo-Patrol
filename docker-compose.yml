version: "2"

services:
    robot:
        build: .
        container_name: robot
        command: /bin/bash -c 'source ./devel/setup.bash && rosrun robopatrol turtlebot'
            
    robopatrol:
        build: .
        container_name: robopatrol
